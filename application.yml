
spring:
  profiles:
    active:
    - dev

---
spring:
  profiles: dev
  application:
    name: session
---
spring:
  profiles: test
  application:
    name: test
---
spring:
  profiles: prod
  application:
    name: prod